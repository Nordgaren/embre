[package]
name = "embre-build"
version = "0.1.0"
edition = "2021"

[dependencies]
embre-utils = { path= "../_utils" }
embre-crypt = { path = "../crypt" }
winresource = "0.1.17"
rand = "0.8.5"

[features]
default = []
aes = ["embre-crypt/aes", "embre-crypt/openssl-vendored"]
openssl = ["embre-crypt/openssl"]
openssl-vendored = ["embre-crypt/openssl-vendored"]